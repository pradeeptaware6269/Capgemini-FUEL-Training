// ## Task 8: Named Function
// Create a named function named `calculateArea()` that accepts length and width and returns the area of a rectangle.
// Call the function with different values.

function calculateArea(a,b)
{
    return (a*b);
}

console.log(calculateArea(2,55));